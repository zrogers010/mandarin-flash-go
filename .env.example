# MandarinFlash Environment Configuration
# Copy this file to .env and fill in your values

# --- Application ---
ENVIRONMENT=development          # development | production
PORT=8080

# --- Domain (production only) ---
# Your domain name, used for SSL cert paths and CORS
DOMAIN=mandarinflash.com

# --- Database (PostgreSQL) ---
DB_HOST=localhost                # Use 'postgres' when running in Docker
DB_PORT=5432
DB_NAME=chinese_learning
DB_USER=postgres
DB_PASSWORD=password             # CHANGE IN PRODUCTION (generate: openssl rand -base64 24)
DB_SSLMODE=disable

# --- Redis ---
REDIS_HOST=localhost             # Use 'redis' when running in Docker
REDIS_PORT=6379
REDIS_PASSWORD=                  # Set a password in production (generate: openssl rand -base64 24)

# --- JWT ---
# Generate with: openssl rand -hex 32
JWT_SECRET=                      # REQUIRED IN PRODUCTION

# --- CORS ---
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:3000

# --- Frontend URL ---
# Used for email links (verification, password reset)
FRONTEND_URL=http://localhost:3000

# --- Email (AWS SES) ---
# Leave AWS_REGION blank for dev mode (emails logged to console)
# In production on EC2 with an IAM role, only AWS_REGION is needed
# For local testing, also set AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY
AWS_REGION=
# AWS_ACCESS_KEY_ID=              # Optional: auto-detected from IAM role on EC2
# AWS_SECRET_ACCESS_KEY=          # Optional: auto-detected from IAM role on EC2
EMAIL_FROM=noreply@mandarinflash.com
EMAIL_FROM_NAME=MandarinFlash
SUPPORT_EMAIL=support@mandarinflash.com

# --- AI Chat (optional, not yet active) ---
# AI_API_KEY=
# AI_BASE_URL=https://api.openai.com/v1
# AI_MODEL=gpt-4o-mini
