# MandarinFlash Environment Configuration
# Copy this file to .env and fill in your values

# --- Application ---
ENVIRONMENT=development          # development | production
PORT=8080

# --- Database (PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chinese_learning
DB_USER=postgres
DB_PASSWORD=password             # CHANGE IN PRODUCTION
DB_SSLMODE=disable               # Use 'require' or 'verify-full' in production

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# --- JWT ---
# Generate with: openssl rand -hex 32
JWT_SECRET=                      # REQUIRED IN PRODUCTION

# --- CORS ---
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:3000

# --- Frontend URL ---
FRONTEND_URL=http://localhost:3000

# --- Email (SendGrid) ---
# Leave SENDGRID_API_KEY blank for dev mode (emails logged to console)
SENDGRID_API_KEY=
EMAIL_FROM=noreply@mandarinflash.com
EMAIL_FROM_NAME=MandarinFlash
SUPPORT_EMAIL=support@mandarinflash.com
